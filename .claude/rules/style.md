# Code Style Rules
- 2 space indentation. No tabs.
- CSS custom properties for all colours at :root
- Mobile styles first, desktop in @media (min-width: 768px)
- No inline styles except JS-injected dynamic values
- Comments above every major CSS section (/* === HERO === */)
- No colour hex values outside :root — always use var(--name)
