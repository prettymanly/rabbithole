# Content Rules
- "rabbithole" is always lowercase, never capitalised
- ↓ is always the Unicode arrow, never an image or icon font
- Line breaks in headlines use <br> tags deliberately
- Em dashes use — (Unicode directly, not &mdash;)
- Oxford comma always
- Singapore spelling (programme, behaviour, centre)
